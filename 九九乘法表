#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main() {
	int a, b, c;
	for (a = 1; a <= 9; a++) {
		for (b = a; b <= 9; b++) {
			c = a * b;
			printf("%d*%d=%d    ", a, b, c);
		}
		printf("\n");
	}



	return 0;
}
